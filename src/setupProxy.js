// This file is optional and will be loaded before react-scripts start
// It's used to configure the dev server without using deprecated options

module.exports = function(app) {
  // Setup custom middleware here if needed
  // This replaces the deprecated onAfterSetupMiddleware option
};
